{
  "name": "random-choosers",
  "version": "1.0.0",
  "private": true,
  "description": "random.oddspark.dev - a shelf of random choosers. Built-ins run in the browser; user choosers get an AI-generated list, fixed forever.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "node test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
